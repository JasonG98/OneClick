# 实施记录

## 2026-09-22：精简开发与发布流程

减少入口与维护量：

- 脚本从 9 个收成 5 个（1614 → 597 行）；删除图标生成器、公共 shell 库及发布包装脚本。
- `release.py` 统一构建、签名/版本/架构检查、DMG、校验和、Cask 与草稿上传。
- CI 保留检查、发布两个 workflow；发布合为一个任务，去掉跨任务产物传递。
- 构建默认验证启动；删除日志/遥测/单独验证模式。保留扩展恢复。
- 卸载保留当前数据的归属检查，移除旧容器扫描和仓库清理选项。
- 图标目录原样迁入 `assets/`；重写 README 与操作文档，旧记录归档。

取舍：保持测试单入口，不再增加轻量/专项/全量模式；本次不改 Swift 产品行为。
单任务发布省去独立产物提交号文件，使用同一次 tag checkout 完成测试、打包和上传。

验证：最终 96 项 Swift 测试与 22 项脚本测试通过，主应用及扩展 arm64 无签名编译通过。
外层沙盒限制 Swift 宏进程，获准在沙盒外重跑后通过。图标 PNG 字节保持一致，工程连续生成一致。
本地 Release 构建、ad hoc 签名/架构检查、DMG 校验和 Cask 语法通过；产物在 `.build/simplify-release/`。
最终复查补上未跟踪文件阻止草稿上传、扩展注册查询失败仍重试的回归测试；两项均先复现失败再修复。
Markdown 文件链接、工程重复生成、workflow YAML 解析和 `git diff --check` 通过。
没有触发远程发布或重新执行桌面验收。

[此前实施记录](history/implementation-log.md)保留历史决策与验证，不作为当前命令说明。
